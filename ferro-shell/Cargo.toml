[package]
name = "ferro-shell"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.35", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
redb = "2.1.0"
csv = "1.3.0"
ferro-core = { path = "../ferro-core" }
